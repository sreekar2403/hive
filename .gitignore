# Dependencies
node_modules/
package-lock.json
yarn.lock

# pnpm-lock.yaml is committed intentionally — it's the only thing that makes installs reproducible.
pnpm-lock.yaml

# Build outputs
dist/
packages/*/dist/
packages/client/electron/dist/
*.tsbuildinfo

# electron-builder packaged app output
release/

# Testing
coverage/
.vitest/

# Caching
.hive-cache/
.cache/

# Runtime data (SQLite db + WAL/SHM files created on server boot)
storage/

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Temporary
.tmp/
tmp/

# Agent/skill related directories and files - keep docs/ tracked for OSS, ignore only ephemeral agent state
.hive/
.superpowers/
.docs/
.claude/
.claude/skills/
.claude/prompts/
.claude/settings/
CLAUDE.md

# Wayfinder research/tickets (entire directory)
.hive/wayfinder/

# Superpowers specs/plans (entire directory) — keep top-level docs/ tracked
docs/superpowers/

# Agent skills and prompts directories
.agent/
.skills/
.prompt-templates/

# Other agent frameworks
.pi/
.venv/
.env.agent
.playwright-mcp/
# tsc output from a stray root build — the root tsconfig has no rootDir, so
# `tsc` here drops .js/.d.ts/.map next to the sources it compiled.
/vitest.config.js
/vitest.config.js.map
/vitest.config.d.ts
/vitest.config.d.ts.map

# Second Brain memory, written by agent runs against this repo as a project.
# Per-machine learning state, not source — and it changes on every run.
mem/
docs/ToDo.md
hive.config.json
